apiVersion: v1
kind: Service

metadata:
  name: employee-directory-service

spec:
  type: LoadBalancer

  selector:
    app: employee-directory

  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080